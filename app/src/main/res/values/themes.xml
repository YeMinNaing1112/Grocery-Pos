<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GroceryPOS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.GroceryPOS" parent="Base.Theme.GroceryPOS" />

    <style name="MyTitleTextAppearance" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/text_regular_3x</item>
        <item name="android:textStyle">bold</item>
        <!-- Add more attributes as needed -->
    </style>

//For ImageViewRoundedSquare
    <style name="RoundedSquare">
        <item name="cornerSize">@dimen/margin_medium_2</item>
        <item name="cornerFamily">rounded</item>
    </style>
</resources>